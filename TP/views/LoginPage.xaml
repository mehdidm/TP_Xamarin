<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TP.views.LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid Margin="20 ,0,20,0" VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>

                <Entry Placeholder="Email" FontSize="16" x:Name="EntryEmail" Grid.Row="0"/>
                <Entry Placeholder="Password" IsPassword="True" FontSize="16" x:Name="EntryPassword" Grid.Row="1"/>
                <Button Text="Connexion"  TextColor="White" BackgroundColor="#3d8ee7" Grid.Row="2"  x:Name="bnt1"/>
                <Button Text="Inscription" Grid.Row="3" Clicked="Button_Clicked" />

                

            </Grid>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>